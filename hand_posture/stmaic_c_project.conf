{
  "version": "0.1",
  "description": "Hand posture recognition project",
  "env": {
    "ProjectFolder": "${ProjectFolder}",
    "STM32CubeProgrammer": "${STM32CubeProgrammer}",
    "STM32Make": "${STM32Make}",
    "cwd": "${ProjectFolder}"
  },
  "configurations": [
    {
      "name": "Default",
      "builder": "makefile",
      "series": "STM32F4",
      "board": "NUCLEO-F401RE",
      "cwd": "${ProjectFolder}",
      "toolchain": "gcc"
    }
  ]
}
